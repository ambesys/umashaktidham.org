#############################################
# UMA SHAKTI DHAM - Repository root .htaccess
# This file is intended to block direct web access to the repository root
# (if someone accidentally places the project root inside webroot).
#############################################

# Deny all access to files in the repository root (Apache 2.4+ / 2.2 fallback)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

# If this file is accidentally deployed into the site webroot it will prevent
# exposure of non-public files. If you purposely serve this repo root, remove
# this file and instead place a tailored public/.htaccess under your webroot.

